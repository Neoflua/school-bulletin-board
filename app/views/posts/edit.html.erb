<div class="row">
  <div class="col-12">
    <nav aria-label="breadcrumb" class="mb-4">
      <ol class="breadcrumb">
        <li class="breadcrumb-item">
          <%= link_to "🏠 ホーム", root_path, class: "text-decoration-none" %>
        </li>
        <li class="breadcrumb-item">
          <%= link_to @post.category.name, category_path(@post.category), class: "text-decoration-none" %>
        </li>
        <li class="breadcrumb-item">
          <%= link_to @post.title, post_path(@post), class: "text-decoration-none" %>
        </li>
        <li class="breadcrumb-item active" aria-current="page">
          <i class="fas fa-edit me-1"></i>
          投稿を編集
        </li>
      </ol>
    </nav>

    <div class="card fade-in-up">
      <div class="card-body">
        <h1 class="card-title">
          <i class="fas fa-edit text-primary me-2"></i>
          投稿を編集
        </h1>
        <p class="text-muted">
          <i class="fas fa-folder me-1"></i>
          カテゴリ: <%= @post.category.name %>
        </p>

        <%= form_with model: @post, local: true do |f| %>
          <% if @post.errors.any? %>
            <div class="alert alert-danger fade-in-up">
              <h4>
                <i class="fas fa-exclamation-triangle me-2"></i>
                <%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:
              </h4>
              <ul>
                <% @post.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="mb-3">
            <%= f.label :title, "タイトル", class: "form-label" %>
            <%= f.text_field :title, class: "form-control", required: true, placeholder: "投稿のタイトルを入力してください..." %>
            <div class="form-text">
              <i class="fas fa-info-circle me-1"></i>
              5文字以上200文字以下で入力してください
            </div>
          </div>

          <div class="mb-3">
            <%= f.label :content, "内容", class: "form-label" %>
            <%= f.text_area :content, class: "form-control", rows: 10, required: true, placeholder: "投稿の内容を入力してください..." %>
            <div class="form-text">
              <i class="fas fa-info-circle me-1"></i>
              10文字以上で入力してください
            </div>
          </div>

          <div class="mb-3">
            <div class="form-check">
              <%= f.check_box :anonymous, class: "form-check-input", id: "post_anonymous" %>
              <%= f.label :anonymous, "匿名で投稿", class: "form-check-label", for: "post_anonymous" %>
            </div>
          </div>

          <div class="mb-3" id="author_name_field">
            <%= f.label :author_name, "名前（任意）", class: "form-label" %>
            <%= f.text_field :author_name, class: "form-control", placeholder: "お名前を入力してください..." %>
          </div>

          <div class="d-flex gap-2">
            <%= f.submit "💾 更新する", class: "btn btn-primary" %>
            <%= link_to "❌ キャンセル", post_path(@post), class: "btn btn-secondary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const anonymousCheckbox = document.getElementById('post_anonymous');
  const authorNameField = document.getElementById('author_name_field');
  
  if (anonymousCheckbox) {
    anonymousCheckbox.addEventListener('change', function() {
      if (this.checked) {
        authorNameField.style.display = 'none';
      } else {
        authorNameField.style.display = 'block';
      }
    });
  }
});
</script>

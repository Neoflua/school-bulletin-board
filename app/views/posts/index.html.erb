<div class="row">
  <div class="col-12">
    <nav aria-label="breadcrumb" class="mb-4">
      <ol class="breadcrumb">
        <li class="breadcrumb-item">
          <%= link_to "🏠 ホーム", root_path, class: "text-decoration-none" %>
        </li>
        <li class="breadcrumb-item active" aria-current="page">
          <i class="fas fa-file-alt me-1"></i>
          投稿一覧
        </li>
      </ol>
    </nav>

    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="mb-0">
        <i class="fas fa-file-alt text-primary me-2"></i>
        投稿一覧
      </h1>
      <%= link_to "📝 新規投稿", new_category_post_path(@category), class: "btn btn-primary" %>
    </div>

    <% if @posts.any? %>
      <div class="row">
        <% @posts.each do |post| %>
          <div class="col-12 mb-3">
            <div class="post-card fade-in-up">
              <div class="card-body">
                <div class="d-flex justify-content-between align-items-start">
                  <h5 class="card-title">
                    <%= link_to post.title, post_path(post) %>
                  </h5>
                  <div class="d-flex align-items-center gap-2">
                    <% if post.anonymous? %>
                      <span class="anonymous-badge">
                        <i class="fas fa-user-secret me-1"></i>匿名
                      </span>
                    <% end %>
                    <div class="btn-group" role="group">
                      <%= link_to edit_post_path(post), class: "btn btn-outline-primary btn-sm" do %>
                        <i class="fas fa-edit me-1"></i>
                      <% end %>
                      <%= link_to post_path(post), 
                          method: :delete, 
                          data: { 
                            confirm: "この投稿を削除しますか？\nこの操作は取り消せません。",
                            turbo_method: :delete 
                          }, 
                          class: "btn btn-outline-danger btn-sm" do %>
                        <i class="fas fa-trash me-1"></i>
                      <% end %>
                    </div>
                  </div>
                </div>
                <p class="card-text">
                  <%= truncate(post.content, length: 200) %>
                </p>
                <div class="post-meta">
                  <div class="d-flex align-items-center">
                    <i class="fas fa-user me-2 text-muted"></i>
                    <span class="text-muted">
                      <%= post.anonymous? ? "匿名" : post.author_name %> | 
                      <i class="fas fa-folder me-1"></i>
                      <%= post.category.name %> | 
                      <i class="fas fa-clock me-1"></i>
                      <%= post.created_at.strftime("%Y/%m/%d %H:%M") %>
                    </span>
                  </div>
                  <div class="d-flex align-items-center">
                    <i class="fas fa-comments me-1 text-muted"></i>
                    <span class="text-muted">
                      <%= pluralize(post.comments.count, "コメント") %>
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>

      <div class="d-flex justify-content-center mt-4">
        <%= paginate @posts %>
      </div>
    <% else %>
      <div class="alert alert-info fade-in-up">
        <i class="fas fa-info-circle me-2"></i>
        このカテゴリにはまだ投稿がありません。
        <%= link_to "最初の投稿をしてみませんか？", new_category_post_path(@category), class: "alert-link" %>
      </div>
    <% end %>
  </div>
</div>
